spring:
  application:
    name: configclient
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888/

management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        includes: "*"